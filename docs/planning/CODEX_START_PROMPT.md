# 우리집식당 UI 구현 요청

첨부한 9개 화면 시안을 기준으로 **실제로 탐색하고 조작할 수 있는 브라우저 저장형 웹 시제품**을 제작해줘.

## 1. 가장 중요한 전제

- 이 프로젝트의 이름은 `우리집식당`이다.
- 목적은 가족 식사에서 반복되는 질문과 인원 조율을 줄이는 것이다.
- 핵심 흐름은 `식사 만들기 → 링크 공유 → 먹어요/안 먹어요 응답 → 인원 확정 → 기록 → 다시 만들기`다.
- 운영자/참여자 모드 토글을 만들지 않는다. 권한과 현재 상황에 맞는 행동을 화면에서 자연스럽게 보여준다.
- 참여자는 회원가입하지 않고 공유 링크에서 30초 이내에 응답할 수 있어야 한다.
- 이번 버전은 실제 서버나 DB를 연결하지 않고 `localStorage`를 사용한다.
- 첨부된 `SKILL.md`가 있다면 먼저 전부 읽고 Ponytail 원칙을 적극 적용한다. 가장 단순하게 작동하는 구조를 우선하며, 불필요한 라이브러리·추상화·파일 분리를 추가하지 않는다.

## 2. 기술 및 작업 원칙

- 기존 프로젝트가 있으면 그 구조와 패키지를 먼저 확인하고 이어서 작업한다.
- 프로젝트에 `.openai/hosting.json`이 있으면 Sites 프로젝트이므로 해당 워크플로를 사용한다.
- 기존 프레임워크가 없다면 Next.js App Router + TypeScript + CSS Modules 또는 단순 전역 CSS로 구성한다.
- UI 라이브러리를 새로 설치하지 말고 기본 HTML, CSS, React로 구현한다.
- 아이콘은 기존 패키지가 있을 때만 사용하고, 없다면 접근 가능한 텍스트·CSS 또는 간단한 인라인 SVG를 사용한다.
- 이미지 시안의 AI 생성 문구나 음식 사진을 그대로 진실로 간주하지 않는다. 화면 구조, 정보 우선순위, 버튼 위치, 분위기를 참고하고 아래 요구사항을 기준으로 구현한다.
- 모바일 우선으로 만들되 데스크톱에서는 중앙 최대 폭 레이아웃으로 자연스럽게 보이게 한다.
- 버튼, 입력창, 패널에 키보드 포커스와 접근 가능한 이름을 제공한다.
- 색상만으로 상태를 구분하지 않는다.
- 비즈니스 로직이 있는 부분에는 최소 1개의 실행 가능한 검증 또는 테스트를 남긴다.

## 3. 화면 파일의 의미

첨부 이미지 9장을 9개의 고립된 HTML 문서로 만들지 않는다. 다음처럼 페이지, 오버레이, 상태로 해석한다.

| 이미지 | 구현 형태 | 권장 경로/상태 |
|---|---|---|
| `01_home_corrected.png` | 로그인 홈 페이지 | `/` |
| `02_my_table.png` | 내 식탁 페이지 | `/tables` |
| `03_calendar.png` | 달력 페이지 | `/calendar` |
| `04_records.png` | 기록 페이지 | `/records` |
| `05_hamburger_menu.png` | 공통 레이아웃 위 드로어 | 메뉴 버튼으로 열기 |
| `06_notifications.png` | 공통 레이아웃 위 알림 패널 | 종 버튼으로 열기 |
| `07_household_selector.png` | 상단 우리집 선택 팝오버 | `우리집 ▼`로 열기 |
| `08_create_meal.png` | 식사 만들기 페이지 | `/meals/new` |
| `09_empty_home.png` | 홈의 빈 상태 | 식사 데이터가 없을 때 `/` |

추가로 핵심 사용자 흐름 완성을 위해 아래 비로그인 응답 화면도 최소한으로 구현한다.

- `/respond/[mealId]`: 식사 정보 확인, 이름 확인, 먹어요/안 먹어요 선택, 도착 시간 선택 입력, 제출
- 제출 완료 상태: 응답 내용 표시와 응답 수정 버튼
- 비로그인 화면에는 로그인 사용자용 하단 내비게이션과 햄버거 메뉴를 표시하지 않는다.

## 4. 공통 내비게이션

상단 바:

- 왼쪽: `우리집식당` 브랜드
- 가운데 또는 자연스러운 위치: `우리집 ▼` 가구 선택
- 오른쪽: 알림, 햄버거 메뉴
- 관리자 배지나 관리자/참여자 전환 토글은 만들지 않는다.

하단 내비게이션은 정확히 다음 순서로 고정한다.

1. 홈
2. 내 식탁
3. 가운데 돌출형 `+` 식사 만들기
4. 달력
5. 기록

`+`는 선택되는 탭이 아니라 식사 만들기 행동이다. 화면 오른쪽에 별도의 플로팅 `+` 버튼을 중복 배치하지 않는다.

## 5. 홈에 보여줄 정보

홈은 기능 포털이 아니라 **오늘 식사와 지금 해야 할 일**을 가장 먼저 보여줘야 한다.

- 오늘의 식탁 또는 오늘 예정된 식사
- 먹는 사람, 안 먹는 사람, 미응답 인원
- 현재 사용자에게 필요한 행동: 응답하기, 미응답자에게 알리기, 인원 확정 등
- 다가오는 식사와 미리 응답하기
- 가족이 남긴 먹고 싶은 메뉴
- 지난 식사에서 다시 만들기
- 데이터가 없으면 `09_empty_home.png`처럼 첫 식사 만들기를 안내

홈에 전체 기능 타일, 상시 밀키트 광고, 냉장고 전체 콘텐츠를 넣지 않는다.

## 6. 햄버거 메뉴 분류

낮은 빈도의 기능은 아래처럼 드로어 안에 둔다.

- 가족과 가구: 가족 구성원, 공동 관리자, 손님 관리, 가구 초대
- 반복 설정: 요일별 기본 응답, 반복 식사 관리, 자주 가는 곳, 알림 설정
- 살림과 제안: 우리집 냉장고, 밀키트 둘러보기
- 서비스: 계정 설정, 도움말, 개인정보 및 데이터, 로그아웃

사진 기록, 식비 예산, 정산, 냉장고 AI, 실시간 인기 장소 추천은 이번 구현 범위에서 제외한다.

## 7. 로그인과 비로그인 차이

- 로그인 사용자: 우리집 구성원으로 기억되며 홈, 내 식탁, 달력, 기록, 알림, 메뉴를 이용한다.
- 비로그인 사용자: 공유받은 특정 식사의 응답만 가능하다.
- 비로그인 응답은 해당 식사 ID와 임시 참여자 ID로 localStorage에 저장한다.
- 비로그인 사용자는 식사를 생성하거나 인원을 확정할 수 없다.
- 시제품의 로그인은 실제 인증이 아니라 브라우저에 저장된 프로필 선택 방식으로 단순화한다.

## 8. localStorage 데이터와 동작

최소 데이터:

- 현재 프로필과 가구
- 가족 구성원
- 식사: 날짜, 시간, 메뉴, 메모, 생성자, 상태
- 식사별 응답: 먹어요/안 먹어요, 도착 시간, 응답자
- 메뉴 요청
- 알림 읽음 상태

필수 동작:

- 식사 생성 후 홈, 내 식탁, 달력에 즉시 반영
- 먹어요/안 먹어요 응답 저장 및 수정
- 응답 집계와 미응답 인원 계산
- 식사 인원 확정
- 확정된 식사를 기록에서 확인
- 기록의 식사를 복제하여 새 식사 만들기
- 알림 읽음 처리
- 가구 선택 팝오버 열기/닫기
- 햄버거와 알림 패널은 ESC, 바깥 클릭, 닫기 버튼으로 닫기
- 저장 데이터가 없을 때 빈 홈 표시
- 데모 확인을 위한 초기 예시 데이터와 데이터 초기화 기능 제공

## 9. 디자인 방향

- 따뜻한 미색 배경, 흰 카드, 짙은 차콜 텍스트
- 포인트 컬러는 차분한 딥 올리브 그린, 보조 포인트는 소량의 번트 오렌지
- 얇은 테두리, 과하지 않은 그림자, 큰 한글 글자와 높은 대비
- 가족 식탁의 따뜻함은 유지하되 배달앱, 주문 POS, 결제 서비스처럼 보이지 않게 한다.
- 시안의 모바일 정보 위계를 유지하되 정확한 텍스트, 간격, 컴포넌트 상태는 코드에서 정돈한다.
- 체크무늬 가짜 투명 배경은 사용하지 않는다.

## 10. 구현 순서

1. 현재 프로젝트와 첨부 파일 확인
2. 공통 디자인 토큰과 상단·하단 내비게이션
3. 홈과 빈 홈
4. 내 식탁, 달력, 기록
5. 식사 만들기
6. 햄버거, 알림, 가구 선택 패널
7. 비로그인 응답과 제출 완료
8. localStorage 연결
9. 모바일·데스크톱·키보드 접근성 검수
10. 실행 및 테스트 후 결과 보고

## 11. 완료 보고 형식

작업 후 다음을 정확히 알려줘.

- 구현한 화면과 기능
- 구현하지 않았거나 임시 처리한 기능
- 주요 파일 구조
- 실행 방법
- localStorage 데이터 구조
- 테스트 결과
- 화면별 확인 가능한 URL
- 다음 단계에서 실제 인증·DB를 붙일 때 바꿔야 할 부분

먼저 장황한 재기획서를 작성하지 말고 현재 파일을 확인한 뒤 구현에 착수해줘. 기존 사용자 파일이나 변경사항을 덮어쓰거나 삭제하지 말고, 관련 없는 코드는 건드리지 마.
